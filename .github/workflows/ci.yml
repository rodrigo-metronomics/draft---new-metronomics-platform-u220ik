# name: Continuous Integration

# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main, develop]
#   workflow_dispatch:

# env:
#   NODE_VERSION: '18'

# jobs:
#   lint:
#     name: Lint
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         project: [backend, web]
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
      
#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: npm
#           cache-dependency-path: src/${{ matrix.project }}/package-lock.json
      
#       - name: Install dependencies
#         working-directory: src/${{ matrix.project }}
#         run: npm ci
      
#       - name: Run linting
#         working-directory: src/${{ matrix.project }}
#         run: npm run lint
      
#       - name: Check formatting
#         working-directory: src/${{ matrix.project }}
#         run: npm run format -- --check

#   unit-tests:
#     name: Unit Tests
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         project: [backend, web]
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
      
#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: npm
#           cache-dependency-path: src/${{ matrix.project }}/package-lock.json
      
#       - name: Install dependencies
#         working-directory: src/${{ matrix.project }}
#         run: npm ci
      
#       - name: Generate Prisma client
#         if: matrix.project == 'backend'
#         working-directory: src/${{ matrix.project }}
#         run: npx prisma generate
      
#       - name: Run unit tests
#         working-directory: src/${{ matrix.project }}
#         run: npm test -- --coverage
      
#       - name: Upload coverage report
#         uses: actions/upload-artifact@v3
#         with:
#           name: ${{ matrix.project }}-coverage
#           path: src/${{ matrix.project }}/coverage
#           retention-days: 7

#   integration-tests:
#     name: Integration Tests
#     runs-on: ubuntu-latest
#     needs: [unit-tests]
#     services:
#       postgres:
#         image: postgres:15
#         env:
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: metronomics_test
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#       redis:
#         image: redis:7
#         ports:
#           - 6379:6379
#         options: >-
#           --health-cmd "redis-cli ping"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
      
#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: npm
#           cache-dependency-path: src/backend/package-lock.json
      
#       - name: Install dependencies
#         working-directory: src/backend
#         run: npm ci
      
#       - name: Generate Prisma client
#         working-directory: src/backend
#         run: npx prisma generate
      
#       - name: Run migrations
#         working-directory: src/backend
#         env:
#           DATABASE_URL: postgresql://postgres:postgres@localhost:5432/metronomics_test
#         run: npx prisma migrate deploy
      
#       - name: Run integration tests
#         working-directory: src/backend
#         env:
#           DATABASE_URL: postgresql://postgres:postgres@localhost:5432/metronomics_test
#           REDIS_URL: redis://localhost:6379
#           NODE_ENV: test
#           JWT_SECRET: test-secret
#           FIREBASE_PROJECT_ID: test-project
#         run: npm run test:integration

#   e2e-tests:
#     name: End-to-End Tests
#     runs-on: ubuntu-latest
#     needs: [unit-tests]
#     services:
#       postgres:
#         image: postgres:15
#         env:
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: metronomics_test
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#       redis:
#         image: redis:7
#         ports:
#           - 6379:6379
#         options: >-
#           --health-cmd "redis-cli ping"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
      
#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: npm
#           cache-dependency-path: src/web/package-lock.json
      
#       - name: Install dependencies
#         working-directory: src/web
#         run: npm ci
      
#       - name: Install Cypress binary
#         working-directory: src/web
#         run: npx cypress install
      
#       - name: Build web app
#         working-directory: src/web
#         env:
#           VITE_API_URL: http://localhost:3000
#           VITE_FIREBASE_CONFIG: '{"apiKey":"test-key","authDomain":"test.firebaseapp.com","projectId":"test-project","storageBucket":"test.appspot.com","messagingSenderId":"123456789","appId":"1:123456789:web:abcdef"}'
#         run: npm run build
      
#       - name: Start backend server
#         working-directory: src/backend
#         run: npm ci && npx prisma generate && npm run build && node dist/server.js &
#         env:
#           NODE_ENV: test
#           PORT: '3000'
#           DATABASE_URL: postgresql://postgres:postgres@localhost:5432/metronomics_test
#           REDIS_URL: redis://localhost:6379
#           JWT_SECRET: test-secret
#           FIREBASE_PROJECT_ID: test-project
      
#       - name: Start web server
#         working-directory: src/web
#         run: npx vite preview --port 8080 &
      
#       - name: Wait for servers to start
#         run: sleep 10
      
#       - name: Run Cypress tests
#         working-directory: src/web
#         env:
#           CYPRESS_BASE_URL: http://localhost:8080
#         run: npx cypress run
      
#       - name: Upload Cypress artifacts
#         uses: actions/upload-artifact@v3
#         if: always()
#         with:
#           name: cypress-artifacts
#           path: src/web/cypress/videos
#           retention-days: 7

#   build:
#     name: Build
#     runs-on: ubuntu-latest
#     needs: [lint, unit-tests, integration-tests, e2e-tests]
#     strategy:
#       matrix:
#         project: [backend, web]
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
      
#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: npm
#           cache-dependency-path: src/${{ matrix.project }}/package-lock.json
      
#       - name: Install dependencies
#         working-directory: src/${{ matrix.project }}
#         run: npm ci
      
#       - name: Generate Prisma client
#         if: matrix.project == 'backend'
#         working-directory: src/${{ matrix.project }}
#         run: npx prisma generate
      
#       - name: Build application
#         working-directory: src/${{ matrix.project }}
#         env:
#           VITE_API_URL: https://api.metronomics.io
#           VITE_FIREBASE_CONFIG: ${{ secrets.FIREBASE_CONFIG_DEV }}
#         run: npm run build
      
#       - name: Upload build artifacts
#         uses: actions/upload-artifact@v3
#         with:
#           name: ${{ matrix.project }}-build
#           path: src/${{ matrix.project }}/${{ matrix.project == 'backend' && 'dist' || 'dist' }}
#           retention-days: 7

#   docker-build-test:
#     name: Test Docker Build
#     runs-on: ubuntu-latest
#     needs: [build]
#     if: github.event_name == 'pull_request'
#     strategy:
#       matrix:
#         dockerfile: [backend, web]
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
      
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2
      
#       - name: Build Docker image
#         uses: docker/build-push-action@v4
#         with:
#           context: .
#           file: infrastructure/docker/Dockerfile.${{ matrix.dockerfile }}
#           push: false
#           tags: metronomics-${{ matrix.dockerfile }}:test
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           build-args: |
#             VITE_API_URL=https://api.metronomics.io
#             VITE_FIREBASE_CONFIG=${{ secrets.FIREBASE_CONFIG_DEV }}

#   notify-success:
#     name: Notify Success
#     runs-on: ubuntu-latest
#     needs: [lint, unit-tests, integration-tests, e2e-tests, build, docker-build-test]
#     if: success() && github.event_name != 'pull_request'
#     steps:
#       - name: Generate summary
#         run: |
#           echo "## CI Pipeline Successful! ✅" >> $GITHUB_STEP_SUMMARY
#           echo "All checks have passed for commit ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

#   notify-failure:
#     name: Notify Failure
#     runs-on: ubuntu-latest
#     needs: [lint, unit-tests, integration-tests, e2e-tests, build, docker-build-test]
#     if: failure() && github.event_name != 'pull_request'
#     steps:
#       - name: Generate summary
#         run: |
#           echo "## CI Pipeline Failed! ❌" >> $GITHUB_STEP_SUMMARY
#           echo "One or more checks have failed for commit ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
