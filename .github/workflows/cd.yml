# name: Continuous Deployment

# on:
#   workflow_dispatch:
#     inputs:
#       environment:
#         description: 'Deployment environment'
#         type: choice
#         options:
#           - development
#           - staging
#           - production
#         required: true
#         default: 'development'
#       deploy_type:
#         description: 'Type of deployment'
#         type: choice
#         options:
#           - full
#           - app-only
#           - infra-only
#         required: true
#         default: 'full'
#       skip_tests:
#         description: 'Skip post-deployment tests'
#         type: boolean
#         required: false
#         default: false
#   push:
#     branches:
#       - main
#     paths:
#       - 'src/**'
#       - 'infrastructure/**'
#       - '!**/*.md'
#       - '!**/*.test.ts'
#       - '!**/*.test.tsx'
#   workflow_run:
#     workflows:
#       - 'Continuous Integration'
#     branches:
#       - main
#     types:
#       - completed

# env:
#   NODE_VERSION: '18'
#   AWS_REGION: 'us-east-1'
#   ECR_REPOSITORY_BACKEND: 'metronomics-backend'
#   ECR_REPOSITORY_WEB: 'metronomics-web'
#   TF_VERSION: '1.5.0'

# jobs:
#   prepare:
#     name: Prepare Deployment
#     runs-on: ubuntu-latest
#     outputs:
#       environment: ${{ steps.set-env.outputs.environment }}
#       deploy_type: ${{ steps.set-env.outputs.deploy_type }}
#       skip_tests: ${{ steps.set-env.outputs.skip_tests }}
#       version: ${{ steps.set-version.outputs.version }}
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3
#         with:
#           fetch-depth: 0

#       - name: Set deployment parameters
#         id: set-env
#         run: |
#           echo "environment=${{ github.event.inputs.environment || 'development' }}" >> $GITHUB_OUTPUT
#           echo "deploy_type=${{ github.event.inputs.deploy_type || 'full' }}" >> $GITHUB_OUTPUT
#           echo "skip_tests=${{ github.event.inputs.skip_tests || 'false' }}" >> $GITHUB_OUTPUT

#       - name: Set version
#         id: set-version
#         run: |
#           echo "version=$(date +'%Y%m%d%H%M%S')-$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#   build-and-push:
#     name: Build and Push Docker Images
#     runs-on: ubuntu-latest
#     needs: [prepare]
#     if: "needs.prepare.outputs.deploy_type == 'full' || needs.prepare.outputs.deploy_type == 'app-only'"
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v1

#       - name: Build and push backend image
#         uses: docker/build-push-action@v4
#         with:
#           context: src/backend
#           file: infrastructure/docker/Dockerfile.backend
#           push: true
#           tags: |
#             ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY_BACKEND }}:${{ needs.prepare.outputs.version }}
#             ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY_BACKEND }}:${{ needs.prepare.outputs.environment }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#       - name: Build and push web image
#         uses: docker/build-push-action@v4
#         with:
#           context: src/web
#           file: infrastructure/docker/Dockerfile.web
#           push: true
#           tags: |
#             ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY_WEB }}:${{ needs.prepare.outputs.version }}
#             ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY_WEB }}:${{ needs.prepare.outputs.environment }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           build-args: |
#             FIREBASE_API_KEY=${{ secrets.FIREBASE_API_KEY }}
#             FIREBASE_AUTH_DOMAIN=${{ secrets.FIREBASE_AUTH_DOMAIN }}
#             FIREBASE_PROJECT_ID=${{ secrets.FIREBASE_PROJECT_ID }}
#             FIREBASE_STORAGE_BUCKET=${{ secrets.FIREBASE_STORAGE_BUCKET }}
#             FIREBASE_MESSAGING_SENDER_ID=${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
#             FIREBASE_APP_ID=${{ secrets.FIREBASE_APP_ID }}

#   deploy-infrastructure:
#     name: Deploy Infrastructure
#     runs-on: ubuntu-latest
#     needs: [prepare]
#     if: "needs.prepare.outputs.deploy_type == 'full' || needs.prepare.outputs.deploy_type == 'infra-only'"
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v2
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Terraform Init
#         working-directory: infrastructure/terraform
#         run: terraform init -backend-config=environments/${{ needs.prepare.outputs.environment }}/backend.tfvars

#       - name: Terraform Plan
#         working-directory: infrastructure/terraform
#         run: terraform plan -var-file=environments/${{ needs.prepare.outputs.environment }}/terraform.tfvars -out=tfplan

#       - name: Terraform Apply
#         working-directory: infrastructure/terraform
#         run: terraform apply -auto-approve tfplan

#       - name: Extract Terraform Outputs
#         working-directory: infrastructure/terraform
#         run: terraform output -json > terraform-outputs.json

#       - name: Upload Terraform Outputs
#         uses: actions/upload-artifact@v3
#         with:
#           name: terraform-outputs
#           path: infrastructure/terraform/terraform-outputs.json
#           retention-days: 1

#   deploy-application:
#     name: Deploy Application
#     runs-on: ubuntu-latest
#     needs: [prepare, build-and-push, deploy-infrastructure]
#     if: "needs.prepare.outputs.deploy_type == 'full' || needs.prepare.outputs.deploy_type == 'app-only'"
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Download Terraform Outputs
#         uses: actions/download-artifact@v3
#         with:
#           name: terraform-outputs
#           path: .
#         if: "needs.prepare.outputs.deploy_type == 'full'"

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v1

#       - name: Update ECS Task Definition - Backend
#         run: |
#           aws ecs describe-task-definition --task-definition metronomics-backend-${{ needs.prepare.outputs.environment }} --query taskDefinition > backend-task-def.json
#           jq '.containerDefinitions[0].image = "${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY_BACKEND }}:${{ needs.prepare.outputs.version }}"' backend-task-def.json > new-backend-task-def.json
#           aws ecs register-task-definition --cli-input-json file://new-backend-task-def.json

#       - name: Update ECS Task Definition - Web
#         run: |
#           aws ecs describe-task-definition --task-definition metronomics-web-${{ needs.prepare.outputs.environment }} --query taskDefinition > web-task-def.json
#           jq '.containerDefinitions[0].image = "${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY_WEB }}:${{ needs.prepare.outputs.version }}"' web-task-def.json > new-web-task-def.json
#           aws ecs register-task-definition --cli-input-json file://new-web-task-def.json

#       - name: Deploy Backend Service
#         run: |
#           aws ecs update-service --cluster metronomics-${{ needs.prepare.outputs.environment }} --service metronomics-backend-${{ needs.prepare.outputs.environment }} --task-definition metronomics-backend-${{ needs.prepare.outputs.environment }} --force-new-deployment

#       - name: Deploy Web Service
#         run: |
#           aws ecs update-service --cluster metronomics-${{ needs.prepare.outputs.environment }} --service metronomics-web-${{ needs.prepare.outputs.environment }} --task-definition metronomics-web-${{ needs.prepare.outputs.environment }} --force-new-deployment

#       - name: Wait for services to stabilize
#         run: |
#           aws ecs wait services-stable --cluster metronomics-${{ needs.prepare.outputs.environment }} --services metronomics-backend-${{ needs.prepare.outputs.environment }} metronomics-web-${{ needs.prepare.outputs.environment }}

#   post-deployment-tests:
#     name: Post-Deployment Tests
#     runs-on: ubuntu-latest
#     needs: [prepare, deploy-application]
#     if: "needs.prepare.outputs.skip_tests == 'false' && (needs.prepare.outputs.deploy_type == 'full' || needs.prepare.outputs.deploy_type == 'app-only')"
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: 'npm'
#           cache-dependency-path: 'src/web/package-lock.json'

#       - name: Install Cypress
#         working-directory: src/web
#         run: npm ci && npx cypress install

#       - name: Run Smoke Tests
#         working-directory: src/web
#         env:
#           CYPRESS_BASE_URL: "${{ needs.prepare.outputs.environment == 'production' && 'https://app.metronomics.io' || needs.prepare.outputs.environment == 'staging' && 'https://staging.metronomics.io' || 'https://dev.metronomics.io' }}"
#         run: npx cypress run --spec "cypress/e2e/smoke/**/*.cy.ts"

#       - name: Upload Test Results
#         uses: actions/upload-artifact@v3
#         if: always()
#         with:
#           name: cypress-results
#           path: src/web/cypress/videos
#           retention-days: 7

#   rollback:
#     name: Rollback on Failure
#     runs-on: ubuntu-latest
#     needs: [prepare, deploy-application, post-deployment-tests]
#     if: "failure() && (needs.prepare.outputs.deploy_type == 'full' || needs.prepare.outputs.deploy_type == 'app-only')"
#     steps:
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Get previous task definition - Backend
#         run: |
#           aws ecs describe-services --cluster metronomics-${{ needs.prepare.outputs.environment }} --services metronomics-backend-${{ needs.prepare.outputs.environment }} --query 'services[0].deployments[?status==`PRIMARY`].taskDefinition' --output text > previous-backend-task-def.txt

#       - name: Get previous task definition - Web
#         run: |
#           aws ecs describe-services --cluster metronomics-${{ needs.prepare.outputs.environment }} --services metronomics-web-${{ needs.prepare.outputs.environment }} --query 'services[0].deployments[?status==`PRIMARY`].taskDefinition' --output text > previous-web-task-def.txt

#       - name: Rollback Backend Service
#         run: |
#           aws ecs update-service --cluster metronomics-${{ needs.prepare.outputs.environment }} --service metronomics-backend-${{ needs.prepare.outputs.environment }} --task-definition $(cat previous-backend-task-def.txt) --force-new-deployment

#       - name: Rollback Web Service
#         run: |
#           aws ecs update-service --cluster metronomics-${{ needs.prepare.outputs.environment }} --service metronomics-web-${{ needs.prepare.outputs.environment }} --task-definition $(cat previous-web-task-def.txt) --force-new-deployment

#       - name: Wait for services to stabilize
#         run: |
#           aws ecs wait services-stable --cluster metronomics-${{ needs.prepare.outputs.environment }} --services metronomics-backend-${{ needs.prepare.outputs.environment }} metronomics-web-${{ needs.prepare.outputs.environment }}

#       - name: Notify rollback
#         run: |
#           echo "## Deployment Failed - Rollback Completed ⚠️" >> $GITHUB_STEP_SUMMARY
#           echo "The deployment to ${{ needs.prepare.outputs.environment }} environment has failed and was rolled back to the previous stable version." >> $GITHUB_STEP_SUMMARY

#   notify-success:
#     name: Notify Success
#     runs-on: ubuntu-latest
#     needs: [prepare, deploy-application, post-deployment-tests]
#     if: "success() && (needs.prepare.outputs.deploy_type == 'full' || needs.prepare.outputs.deploy_type == 'app-only')"
#     steps:
#       - name: Generate summary
#         run: |
#           echo "## Deployment Successful! ✅" >> $GITHUB_STEP_SUMMARY
#           echo "Successfully deployed version ${{ needs.prepare.outputs.version }} to ${{ needs.prepare.outputs.environment }} environment." >> $GITHUB_STEP_SUMMARY

#   notify-infra-success:
#     name: Notify Infrastructure Success
#     runs-on: ubuntu-latest
#     needs: [prepare, deploy-infrastructure]
#     if: "success() && needs.prepare.outputs.deploy_type == 'infra-only'"
#     steps:
#       - name: Generate summary
#         run: |
#           echo "## Infrastructure Deployment Successful! ✅" >> $GITHUB_STEP_SUMMARY
#           echo "Successfully deployed infrastructure to ${{ needs.prepare.outputs.environment }} environment." >> $GITHUB_STEP_SUMMARY
