import { OrganizationService } from './organizationService';
import { organizationService } from './organizationService';
import { TeamService } from './teamService';
import { teamService } from './teamService';

/**
 * Index file that exports the organization and team services for the Metronomics Platform.
 * This file serves as the entry point for organization-related functionality, providing access to organization and team management services throughout the application.
 */

// Export the OrganizationService class
export { OrganizationService };

// Export the organizationService singleton instance
export { organizationService };

// Export the TeamService class
export { TeamService };

// Export the teamService singleton instance
export { teamService };